# reload fish config
abbr -a hey "source ~/.config/fish/config.fish"

# WIP
# git
abbr -a gf git fetch
abbr -a gfm git fetch origin main
abbr -a grbm "git fetch origin main && gir rebase origin/main"
abbr -a grt git restore
abbr -a gs git status

